int a,b;


int gcd(int x,int y)
{
	//write(x);
	//write(y);
	if(!y) {
		//write(1000);
		return x;
	}
	//write(2000);
	return gcd(y,x%y);
}

int main()
{
	read(a);
	read(b);
	write(gcd(a,b));
	return 0;
}
